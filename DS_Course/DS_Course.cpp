#include <iostream>
#include <vector>
#include <queue>
#include "BinaryHeap.h"
using namespace std;

//BST* buildBalancesBST(vector<int> v) {
//    BST* t = new BST(v[(int)(v.size() / 2)]);
//    vector<int> leftV;
//    vector<int> rightV;
//    for (int i = 0; i < (int)(v.size() / 2); i++)
//    {
//        leftV[i] = v[i];
//    }
//    for (int i = (int)(v.size() / 2) + 1; i < v.size(); i++)
//    {
//        rightV[i] = v[i];
//    }
//    t->left = buildBalancesBST(leftV);
//    t->right = buildBalancesBST(rightV);
//    return t;
//}

int main()
{
    priority_queue<int> q;
    

    return 0;
}